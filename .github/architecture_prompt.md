Build a bicep template that will deploy the following architecture to support a containerized deployment of OpenEMR on Azure.

- Azure Container Apps - Host OpenEMR container with autoscaling and managed ingress.
- Azure Database for MySQL (Flexible Server)
- Networking TLS ingress, Public IP (optional)
- Azure Monitor, App Insights
